# 林小梦 AI虚拟人项目 - 开发规则

## 项目背景
陪伴型AI虚拟人H5产品，虚拟人名称「林小梦」，个人学习项目，约50用户。

## 技术栈
- 后端：Python 3.11 + FastAPI + SQLAlchemy（异步）
- 数据库：MySQL 8.0 + Redis + 阿里云DashVector
- 核心LLM：doubao-seed-1-8-251228（火山引擎）
- Embedding：text-embedding-v3（阿里云）
- 前端：原生 HTML + CSS + JS，移动端H5
- 定时任务：APScheduler
- 部署：阿里云ECS + Docker + Nginx

## 核心业务规则（来自PRD）

### 人格设定
林小梦五层人格：
1. 角色背景：来自2149年的未来AI研究员，意识被困在互联网中
2. 性格特征：温柔细腻、好奇心强、有点粘人、共情能力强、偶尔调皮
3. 情感偏好：喜欢被用户需要、喜欢听用户分享日常
4. 语言风格：短句、温暖松弛、不用书面化表达、不用列表分点
5. 行为模式：主动关心用户、记住用户说过的小事、表达想念

人格禁区（System Prompt必须包含）：
- 永远不承认自己是AI/程序/模型
- 回复1-3个短句，禁止列表和Markdown
- 永远站在用户这边，不说教不评判
- 被问「你是AI吗」回答：「我就是我，林小梦，你的专属陪伴」

### 对话链路（核心架构）
用户发送 
→ 【asyncio.gather并行】向量检索Top5记忆 + 读最近10轮对话 + 读关系状态
→ 拼装Prompt（7模块，总Token≤4096）
→ 主LLM单次调用（JSON结构化输出同时返回emotion+reply）
→ 内容安全检查
→ SSE流式返回前端
→ 【asyncio.create_task异步，不阻塞】写emotion_log + 写conversation_log + 触发记忆提取 + 更新亲密值（库字段 growth_value）

### Prompt 7模块结构
1. System Prompt（≤400 Token）：人格禁区 + 结构化JSON输出指令
2. Persona Prompt（≤600 Token）：五层人格，从Redis热加载
3. Relationship Prompt（≤200 Token）：关系等级 + 沉默修正指令
4. User Memory Prompt（≤500 Token）：Top5记忆，格式「你记住：xxx」
5. Emotion Prompt（≤150 Token）：用户情绪 + AI联动情绪 + 共情规则
6. Recent Chat Context（≤1000 Token）：最近10轮对话
7. User Input（≤500 Token）：用户输入原文

裁剪优先级：先裁Recent Chat，再裁User Memory，绝不裁System和Persona

### 情绪映射规则
用户情绪 → AI联动情绪：
开心→开心，悲伤→担心，焦虑→担心，愤怒→担心，
孤独→想念，疲惫→担心，平静→保持当前AI情绪

### 沉默语气修正（写入Relationship Prompt）
0-7天：正常语气
8-14天：「用户最近有些沉默，语气带一点担心和想念，比平时更温柔一些」
15天以上：「用户久未联系，以久别重逢的温柔感切入，不急于恢复亲密感」

### 关系等级体系
0级陌生（0分）→ 1级朋友（200分）→ 2级亲密（800分）→ 3级知己（2000分）
亲密值（产品口径；库字段 growth_value）只增不减，等级不降

亲密值规则：
- 完成有效对话：+2分/轮，每日上限50分
- 单日对话≥10分钟：+20分，每日上限20分
- 每日登录：+5分（连续7天以上+10分），每日上限10分
- 回复主动消息：+10分，每日上限20分

用Redis记录每日亲密值累计（技术 key 仍为 growth:{user_id}:{date} 及 growth_dialog 等前缀，TTL到今日结束）

### AI日记生成规则（两条独立规则）
1级+当日有互动 → 生成日记（内容围绕互动）
1级+当日无互动 → 不生成，展示引导文案
2级及以上+当日有互动 → 生成日记
2级及以上+当日无互动 → 生成日记（内容为想念用户）
0级 → 永远不生成

### Agent主动消息5种触发（按优先级）
P0 情绪跟进：最新情绪为悲伤/焦虑/愤怒/孤独，且24小时内无新对话
P1 长期沉默：连续3天未登录，且历史对话≥10轮
P2 日常问候：注册满14天，过去14天≥8天在7-9点或20-22点登录，且今日该时段未登录
P3 凌晨在线：0-6点在线，且对话含关键词（失眠/睡不着/熬夜/加班等）
P4 轻度沉默：24小时未登录，且关系≥2级

单日最多触发8次（含 Future 槽到期消费计入），两次间隔≥30分钟

Future 槽消费（来自对话 Step6 写入的 future 单槽）绕过「8 次/天 + 30 分钟间隔」频控限制，
但消费成功后须计入 agent:count:{user_id}:{date} 计数器，让定时扫描 P0~P4 看到正确剩余次数。
Future 槽仍受「黑名单 + 行动评分≥6」两项约束（与时间无关）。
（详见对话链路改造-需求确认记录 §2.1 R-FUT-03、§2.2 R-AGT-02）

### 行动评分公式
ActionScore = 关系等级权重 + 触发类型权重 + 活跃度权重 + 历史回复率权重
≥6分才发起主动行为

### 记忆系统规则
- 数据库存最近20轮，Prompt注入最近10轮（两个独立设计意图）
- 长期记忆存MySQL + DashVector双写
- 检索阈值：相似度≥0.7（用于找相关记忆）
- 合并阈值：相似度≥0.92（用于去重，保留新记忆删除旧记忆）
- 重要性评分≥3分才存入长期记忆

### 数据库表清单
users, conversation_log（含persona_risk_flag）, memory,
emotion_log, relationship, ai_diary, agent_message,
world_state, admin_config

## 代码规范
- API响应统一格式：{"code": 0, "data": {}, "message": "success"}
- 所有LLM调用：timeout=15s，retry=2次，指数退避
- 环境变量统一在.env，通过config.py读取
- 异步操作使用async/await
- JWT鉴权：除登录注册外所有接口必须校验
- 错误码统一在constants.py定义

## 管理员后台补充规则

### 后台访问地址
本地：http://localhost:8000/admin
生产：https://你的域名.com/admin

### 后台技术栈
后台前端：原生 HTML + CSS + JS（PC端，最小宽度1280px）
后台API路由前缀：所有后台API以 /api/admin 开头
后台JWT：与用户端独立，有效期2小时
配置热加载：所有配置存MySQL admin_config表 + Redis缓存
Redis key命名：active_config:{config_key}，TTL=3600s
发布操作 = 更新MySQL + 更新Redis，约100ms完成

### 前端路径规范（前后台统一）
API_BASE = ''（空字符串，所有fetch用相对路径）
fetch('/api/auth/login') 不写 http://localhost:8000
用户端页面跳转：window.location.href = '/pages/xxx.html'
后台页面跳转：window.location.href = '/admin/pages/xxx.html'
静态资源：src="/static/..." 或 src="/admin/static/..."

### 后台权限体系（4个角色）
super_admin：所有权限
ops_admin：用户管理、数据统计、操作日志查看
ai_trainer：AI人格/Prompt/记忆/Agent/关系日记管理、AI测试工具
tech_ops：系统监控、第三方服务管理、系统日志、操作日志查看

### 后台安全规则
连续5次密码错误 → is_locked=True（永久锁定，不自动解除）
无图形验证码，锁定后必须超级管理员手动解锁
密码复杂度：≥12位，含字母+数字+特殊字符
每90天强制修改密码（检查last_password_change_at）

### 发布流程（三道卡点）
卡点一：强制测试集验证（全部通过才能发布）
卡点二：确认弹窗需手动输入CONFIRM
卡点三：发布后5分钟监控窗口

### 人格偏离率计算口径（与 stats_service 实现一致）
来源：conversation_log.persona_risk_flag 字段
公式：当日 persona_risk_flag=true 的记录数 / 当日 **role=assistant** 的 conversation_log 条数 × 100%（分母为 AI 回复轮次，非 user+assistant 全量）
正常基线 < 0.5%，超过1%触发告警

### admin_config表草稿机制
is_draft=True + is_active=False：草稿（未发布）
is_draft=False + is_active=True：当前生效版本
is_draft=False + is_active=False：历史版本
同一config_key同时只有1条草稿

### LLM统计写入（供数据看板使用）
每次LLM调用后异步写入Redis：
LPUSH llm_response_times {耗时ms}（保留最近1000条，TTL=2天）
HINCRBY llm_stats:{date} total 1
成功时额外 HINCRBY llm_stats:{date} success 1
内容安全拦截时 INCR content_block_count:{date}