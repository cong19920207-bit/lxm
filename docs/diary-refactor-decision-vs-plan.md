# 日记改造：决策 vs 修复方案 — 对比报告

> 对照 `**docs/diary-refactor-decisions.md**`（决策）与 `**docs/diary-refactor-plan.md**`（修复方案）。  
> 目的：核对覆盖度、标出缺口与待拍板项。

---

## 一、覆盖矩阵（决策项 → 方案中的落点）


| 决策（decisions §2）        | 修复方案中的体现                         | 结论            |
| ----------------------- | -------------------------------- | ------------- |
| H5 仅用 `data.items`      | 阶段 A 全文                          | ✅ 已覆盖         |
| 进页请求日记列表                | 阶段 A + `initDiary`/`loadDiaries` | ✅ 已覆盖         |
| 日记历史仅 super + ops（O1）   | 阶段 D §2、验收                       | ✅ 已覆盖         |
| 历史字段以契约为准               | 阶段 D §3                          | ✅ 已覆盖         |
| H1：改 Cron 时刻须重启 backend | 阶段 C §4、前置/风险与 TD-013            | ✅ 已覆盖         |
| 配置失败回退硬编码               | 阶段 B §5、测试清单 6                   | ✅ 已覆盖         |
| M2a：手动批跑 + 文档，无 API     | 阶段 E、范围外写明不做 M2b                 | ✅ 已覆盖         |
| 时区/日界本次不改（UTC）          | 方案「不在范围」+ 风险 §1                  | ✅ 已覆盖         |
| 对话同源 / 环境基线             | 方案前置条件 1（对话稳定）                   | ⚠️ 部分覆盖（见 §三） |


---

## 二、一致性与无冲突结论

- **权限 O1**、**重启生效 H1**、**M2a 无后台按钮**、**仅 `items`**、**不回改时区**：两文档一致，无逻辑冲突。
- **实现顺序**：决策 §6 与方案 §六均为 A → B+C → D → E（方案将 B 与 C 拆阶但顺序等价）。
- **TD 映射**：决策 §4 与方案文首、tech-debt 一致。

---

## 三、决策里提到、方案仍偏薄或未闭环的部分


| 编号  | 来源                           | 说明                                                                                                             | 建议                                                                                         |
| --- | ---------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| G1  | 对话「顺序 0」                     | 决策 §1 强调「与对话同源验证」；方案仅写前置「对话稳定」，**未**写清「若对话异常则先停日记改造」的门禁话术。                                                     | 在 `diary-refactor-plan.md` 前置条件加一句 **Gate**：对话不可用时不进入 B/C 验收；或在运维手册注明。                     |
| G2  | Misfire **策略**               | 口头讨论过「策略要定」；决策定 M2a 补跑，**未**定 APScheduler `**misfire_grace_time` / `coalesce`** 是否调整。方案 E 只有手动命令，**未**涉及调度器参数。 | 二选一：**(a)** 维持默认，只靠 M2a；**(b)** 实现时加大 `misfire_grace_time` 减少「错过不跑」。选定后写入 tech-debt 或决策附录。 |
| G3  | `generation_prompt` 形态       | 决策 §5.3 / 方案 B §3 均写「有互动 / 无互动」两分支；**未**预先选定 **单模板 + 代码补段** 还是 **两套模板字段**。                                     | **实现前需拍板**（否则阶段 B 易返工）。建议产品看 `diary-rules.html` 现有占位说明后定稿。                                 |
| G4  | 运营侧 LLM 失败                   | 决策 §5.3：运营手册可查日志/配额；方案仅在风险分散提及，**阶段 E 未**要求写「LLM 失败排查」条目。                                                      | 可选：在 `docs/ops-diary.md` 增加一小节 **故障：批跑成功数为 0**。                                            |
| G5  | `diary-rules.html` 横幅 TD-007 | tech-debt / contract 称页顶横幅提示 TD-007；方案 C 写「保存后重启」Toast。**上线后**需同步改横幅文案（避免仍写「调度未读配置」而实际已读）。                     | 列入 **上线 checklist**，不阻塞开发。                                                                 |


---

## 四、方案里有、决策文档未单独成条但已隐含 OK 的


| 项                       | 说明                                                                                                                              |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 分页用 `total`/`page_size` | 决策 §5.1 曾担心写死 20；**方案 A 已明确要求**用契约字段 → 与决策精神一致，无需改决策。                                                                           |
| `initDiary` 拉关系         | 决策 §5.1 写「未强制进页拉关系」为风险；**当前代码已 `GET /api/relationship/status`**，方案 A 已写明 → 决策 §5.1 该条对**现状**偏保守，可在下次修订决策时注明「已由现有 initDiary 缓解」。 |


---

## 五、仍缺少什么（汇总清单）

### 5.1 文档层面

- **对比报告本文**：补全团队对「还缺什么」的共识（你正在读）。
- **可选**：将 G1/G2/G3 的结论**回写**到 `diary-refactor-decisions.md`「更新记录」或新增「待拍板」小节，避免只在 plan 里。

### 5.2 产品 / 技术拍板（实现前）

~~1. **G3~~**：**已定案** 双字段 `prompt_with_interaction` / `prompt_without_interaction`（见 `diary-refactor-decisions.md` §6）。  
~~2. **G2~~**：**已定案** 维持默认 misfire 参数（见 `docs/ops-diary.md` §6）。

### 5.3 实现后必做（易遗漏）

- **契约**：若接口字段无变更，可不改 `contract.md`；若有行为说明需同步（如「改时刻需重启」），可按 docup 在契约或运维说明中补一句。  
- **横幅 / Toast**：TD-007 清偿后更新 **diary-rules** 页提示（G5）。

### 5.4 明确不在范围（勿与「缺少」混淆）

以下按**既定决策**刻意不做，**不算方案缺口**：Cron 热更新、M2b API、UTC→国内日界、TD-012 运行时读第三方配置。

---

## 六、结论


| 维度               | 评估                                                                                                              |
| ---------------- | --------------------------------------------------------------------------------------------------------------- |
| 决策 vs 方案 **一致性** | 高；无方向性冲突。                                                                                                       |
| **方案对决策的覆盖**     | 核心条目均已落到阶段 A–E。                                                                                                 |
| **仍缺**           | **G2/G3 已定案**（见 `diary-refactor-decisions.md` §6）；**G1/G4** 已落入 `**docs/ops-diary.md`**；**G5** 待 TD-007 清偿后改横幅。 |


完成 **TD-007 实现与横幅替换** 后，文档侧「决策—方案—运维」闭环；代码侧以契约 **TD-007** 行为准。

---

---

## 七、正式「需求对齐检查」结论（已并入修复方案）

以下审查项已**全文吸收**进 `**docs/diary-refactor-plan.md` 的 §〇**，不再在本表重复展开：


| 类别              | 要点                                                                 | 方案中位置             |
| --------------- | ------------------------------------------------------------------ | ----------------- |
| 0 需求对齐          | 内容安全二选一（**中**）；PRD 分支不可误删（**高**）；阶段 D 笔误改为阶段 B 验证                  | §〇.1–§〇.3         |
| 1 兼容性           | `items` 无 API 破坏；`await get_active_config(..., use_cache=...)`     | §〇.4–§〇.5         |
| 2 逻辑            | `code`/`total` 兜底（**中**）；UTC 明示（**中**）；`generation_hour` 脏数据 clamp | §〇.6–§〇.8         |
| 3 异常/XSS        | LLM 失败不写库；后台 `escapeHtml`（**中**）                                   | §〇.9、§四           |
| 4 顺序/共享         | `diary_rules_loader`；批跑竞态说明                                        | §〇.10–§〇.11       |
| 5–8 安全/性能/环境/上线 | 静态文档示例；性能一句；容器名；上线顺序与回滚                                            | §〇.12–§〇.13、§五–§六 |
| 9 可维护性          | 阶段 E 列表编号已修正；contract + tech-debt 同步写进阶段 D 验收                      | §三 E、§〇.13        |


若需追溯**原始审查条文**，以团队内提交的「需求对齐检查」记录为准；**执行以 `diary-refactor-plan.md` 为单一事实源**。

---

*最后更新：2026-04-07（纳入需求对齐审查并入方案）*